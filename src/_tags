<base> or <bioCaml> or <ext/xmlm>: include

<**/*.{ml,mli,byte,native}>: pkg_str, pkg_extlib, pkg_pcre, pkg_sesame, pkg_xmlm, pkg_batteries

<bioCaml/*.cmx>: for-pack(BioCaml)

<app/*.{ml,byte,native}>: pkg_getopt

true: use_menhir, debug
