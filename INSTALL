Biocaml Installation Instructions

Install From Source
==========
Uncompress the source archive, go to the root of the package, then run

    ocaml setup.ml -configure [options]

where choices for options can be determined by running

    ocaml setup.ml -configure --help

Then do

    make
    make install

To compile documentation, do

    make doc

To uninstall, do

    make uninstall

Tags
----
Run 'make TAGS' to generate a TAGS file for use with emacs or
vi/vim. In this case, you will also need the otags tool to be
installed in your path.
